[project]
name = "notice"
version = "0.0.1"
description = "Application for take notes in markdown format"
authors = [
    {name = "infast1k",email = "infast1k@mail.ru"}
]
license = "MIT"
requires-python = ">=3.14"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "ruff (>=0.15.17,<0.16.0)",
    "pre-commit (>=4.6.0,<5.0.0)",
    "pytest (>=9.1.0,<10.0.0)"
]
